export const FORMAT = /([/]tr[:])(.*)(,cm[-]pad[_]resize)/;
export const DEFAULT_HEIGHT = 500;
export const DEFAULT_WIDTH = 500;
export const MAX_WIDTH = 1200;
export const PAD_RESIZE = 'cm-pad_resize';
export const GIF = 'gif';
export const SCALE = 1.5;

export const CROP_STRATEGY = <const>{
  padResize: 'padResize',
};

export type IMAGE_SCALE = 1.5 | 2 | 3 | 4;
